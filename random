from random import random
# Opgave 5.5 
# Stel dat je een geheel toevalgetal tussen 1 en 10 wilt hebben (inclusief 1 en # 10), 
# maar je hebt alleen de random() functie beschikbaar (je mag wel functies uit andere
# modules gebruiken). Hoe doe je dat?
x = random() * 10
x = x + 1
print( "{:.0f}" .format( x ) )
